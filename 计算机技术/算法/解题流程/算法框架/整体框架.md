---
date: 2025-03-26
aliases:
  - 蓝桥杯
tags:
  - 代码框架
---
```cpp
#include <bits/stdc++.h>
using namespace std;

#define fast ios::sync_with_stdio(0);cin.tie(0);cout.tie(0)
#define ffi(a, b) for(int i = a;i < b;i++)
#define ffj(a, b) for(int j = a;j < b;j++)
#define ffk(a, b) for(int k = a;k < b;k++)
#define all(a) (a).begin(),(a).end()

#define x first
#define y second

#define eb emplace_back
#define pb push_back
#define ins insert
#define ps push
#define mmt memset
#define sf sizeof
#define st size_t
//stoi(string)
//greater<int>() 
#define tos to_string
#define pis pair<int, string>
#define pii pair<int, int>
#define piii pair<pair<int , int>, int>
#define piiii pair<pair<int , int>, pair<int , int>>
#define ump unordered_map
#define mp map
#define deq dequeue
#define vec vector
#define bl bool
#define str string

typedef long long ll;typedef float fl;typedef double db;


const int MAX = 0x7fffffff;const int MOD = 1e9+7;const ll INF = 0x3f3f3f3f3f3f3f3f;
const int N = 1e5+7;

void solve(){
	int n;
    cin>>n;
    cout<<
}

signed main()
{
    fast;
    int t=1;
    while(t--){
        solve();
    }
    return 0;
}
```